---
name: generate-graphql
description: Generate GraphQL SDL and related interface definitions for Uptempo.
---

# generate-graphql

Use when clients need flexible queries, graph traversal, or schema-driven composition.

## Output

- write schema files under `graphql/`
- model queries, mutations, and subscriptions only when justified
- document why GraphQL beats REST or gRPC here
